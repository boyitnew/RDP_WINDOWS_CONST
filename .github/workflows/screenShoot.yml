name: RDP screenshoot

on:
  workflow_dispatch:
    inputs:
      target_url:
        description: 'URL to open in Chrome and capture'
        required: false
        default: 'https://www.coinpayu.com/login'

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:
      - name: Configure Core RDP Settings
        run: |
          # Enable Remote Desktop and disable Network Level Authentication (if needed)
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' `
                             -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
                             -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
                             -Name "SecurityLayer" -Value 0 -Force

          # Remove any existing rule with the same name to avoid duplication
          netsh advfirewall firewall delete rule name="RDP-Tailscale"
          
          # For testing, allow any incoming connection on port 3389
          netsh advfirewall firewall add rule name="RDP-Tailscale" `
            dir=in action=allow protocol=TCP localport=3389

          # (Optional) Restart the Remote Desktop service to ensure changes take effect
          Restart-Service -Name TermService -Force

      - name: Create RDP User with Fixed Password
        run: |
          # Set your fixed password here
          $password = "*r96319631P-"
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          New-LocalUser -Name "RDP" -Password $securePass -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member "RDP"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDP"
          
          echo "RDP_CREDS=User: RDP | Password: $password" >> $env:GITHUB_ENV
          
          if (-not (Get-LocalUser -Name "RDP")) {
              Write-Error "User creation failed"
              exit 1
          }

      - name: Install Tailscale
        run: |
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          $installerPath = "$env:TEMP\tailscale.msi"
          
          Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath
          Start-Process msiexec.exe -ArgumentList "/i", "`"$installerPath`"", "/quiet", "/norestart" -Wait
          Remove-Item $installerPath -Force

      - name: Establish Tailscale Connection
        run: |
          # Bring up Tailscale with the provided auth key and set a unique hostname
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-runner-$env:GITHUB_RUN_ID
          
          # Wait for Tailscale to assign an IP
          $tsIP = $null
          $retries = 0
          while (-not $tsIP -and $retries -lt 10) {
              $tsIP = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
              Start-Sleep -Seconds 5
              $retries++
          }
          
          if (-not $tsIP) {
              Write-Error "Tailscale IP not assigned. Exiting."
              exit 1
          }
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV
      
      - name: Verify RDP Accessibility
        run: |
          Write-Host "Tailscale IP: $env:TAILSCALE_IP"
          
          # Test connectivity using Test-NetConnection against the Tailscale IP on port 3389
          $testResult = Test-NetConnection -ComputerName $env:TAILSCALE_IP -Port 3389
          if (-not $testResult.TcpTestSucceeded) {
              Write-Error "TCP connection to RDP port 3389 failed"
              exit 1
          }
          Write-Host "TCP connectivity successful!"

      - name: Set target URL
        shell: pwsh
        run: |
          echo "TARGET_URL=${{ inputs.target_url }}" >> $env:GITHUB_ENV

      - name: Ensure Google Chrome
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $chromeCandidates = @(
            "$env:ProgramFiles\Google\Chrome\Application\chrome.exe",
            "${env:ProgramFiles(x86)}\Google\Chrome\Application\chrome.exe",
            "$env:LOCALAPPDATA\Google\Chrome\Application\chrome.exe"
          )
          $script:chrome = $chromeCandidates | Where-Object { Test-Path $_ } | Select-Object -First 1
          if (-not $script:chrome) {
            Write-Host "Chrome not found, installing..."
            $msiUrl = "https://dl.google.com/dl/chrome/install/googlechromestandaloneenterprise64.msi"
            $msiPath = "$env:TEMP\chrome.msi"
            Invoke-WebRequest -Uri $msiUrl -OutFile $msiPath
            Start-Process msiexec.exe -ArgumentList "/i", "`"$msiPath`"", "/quiet", "/norestart" -Wait
            $script:chrome = $chromeCandidates | Where-Object { Test-Path $_ } | Select-Object -First 1
          }
          if (-not $script:chrome) { throw "Google Chrome not found after install." }
          echo "CHROME_PATH=$script:chrome" >> $env:GITHUB_ENV

      - name: Launch Chrome (normal window)
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $chrome = $env:CHROME_PATH
          if (-not $chrome) { throw "CHROME_PATH not set" }
          $profileDir = "$env:TEMP\chrome-profile"
          if (-not (Test-Path $profileDir)) { New-Item -ItemType Directory -Force -Path $profileDir | Out-Null }
          $args = @(
            "--user-data-dir=$profileDir",
            "--no-first-run",
            "--no-default-browser-check",
            "--start-maximized",
            "--new-window",
            $env:TARGET_URL
          )
          Start-Process -FilePath $chrome -ArgumentList $args | Out-Null

      - name: Capture two desktop screenshots (pre/post 10s)
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'

          function Take-Screen($Path) {
            Add-Type -AssemblyName System.Windows.Forms
            Add-Type -AssemblyName System.Drawing
            $bounds = [System.Windows.Forms.Screen]::PrimaryScreen.Bounds
            $bmp = New-Object System.Drawing.Bitmap($bounds.Width, $bounds.Height)
            $gfx = [System.Drawing.Graphics]::FromImage($bmp)
            $gfx.CopyFromScreen($bounds.Location, [System.Drawing.Point]::Empty, $bounds.Size)
            $bmp.Save($Path, [System.Drawing.Imaging.ImageFormat]::Png)
          }

          # First shot shortly after open
          Start-Sleep -Seconds 5
          $shot1 = Join-Path $PWD 'screenshot_1.png'
          Take-Screen -Path $shot1
          if (!(Test-Path $shot1)) { throw "First screenshot not created" }
          echo "SCREENSHOT1_PATH=$shot1" >> $env:GITHUB_ENV

          # Scroll down and click Cloudflare captcha
          Add-Type -AssemblyName System.Windows.Forms
          
          # Send Page Down key to scroll
          [System.Windows.Forms.SendKeys]::SendWait("{PGDN}")
          Start-Sleep -Seconds 2
          
          # Click captcha position (adjust coordinates based on screenshot)
          $clickX = 512
          $clickY = 500
          [System.Windows.Forms.Cursor]::Position = New-Object System.Drawing.Point($clickX, $clickY)
          
          # Mouse click
          $signature = @'
[DllImport("user32.dll",CharSet=CharSet.Auto,CallingConvention=CallingConvention.StdCall)]
public static extern void mouse_event(long dwFlags, long dx, long dy, long cButtons, long dwExtraInfo);
'@
          $SendMouseClick = Add-Type -MemberDefinition $signature -Name "Win32MouseEvent" -Namespace Win32Functions -PassThru
          $SendMouseClick::mouse_event(0x02, 0, 0, 0, 0)  # Left button down
          $SendMouseClick::mouse_event(0x04, 0, 0, 0, 0)  # Left button up

          # Second shot 10s later
          Start-Sleep -Seconds 10
          $shot2 = Join-Path $PWD 'screenshot_2.png'
          Take-Screen -Path $shot2
          if (!(Test-Path $shot2)) { throw "Second screenshot not created" }
          echo "SCREENSHOT2_PATH=$shot2" >> $env:GITHUB_ENV

      - name: Upload screenshots artifact
        uses: actions/upload-artifact@v4
        with:
          name: timeir-screenshots
          path: |
            screenshot_1.png
            screenshot_2.png
          retention-days: 7
          if-no-files-found: error

      - name: Log screenshots download page
        shell: pwsh
        run: |
          Write-Host "Target URL: $env:TARGET_URL"
          Write-Host "Screenshot #1: $env:SCREENSHOT1_PATH"
          Write-Host "Screenshot #2: $env:SCREENSHOT2_PATH"
          Write-Host "Download from the run page (Artifacts -> timeir-screenshots):"
          Write-Host "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"

      - name: Maintain Connection
        run: |
          Write-Host "`n=== RDP ACCESS ==="
          Write-Host "Address: $env:TAILSCALE_IP"
          Write-Host "Username: RDP"
          Write-Host "Password: $(echo $env:RDP_CREDS)"
          Write-Host "==================`n"
          
          # Keep runner active indefinitely (or until manually cancelled)
          while ($true) {
              Write-Host "[$(Get-Date)] RDP Active - Use Ctrl+C in workflow to terminate"
              Start-Sleep -Seconds 300
          }
